meta {
  name: End conversation
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/conversations/{{conversationId}}/end
  body: text
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
